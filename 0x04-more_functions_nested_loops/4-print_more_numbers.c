#include "main.h"

/**
 * print_most_numbers - prints 0 - 9 except 2, 3
 * Description: prints
 * Return: void
 */

void print_most_numbers(void)
{
	int numb = 0;

	while (numb <= 9)
	{
		if (numb == 2 || a == 4)
		{
			continue;
		}
		num++;
	}
	_putchar('\n');
}