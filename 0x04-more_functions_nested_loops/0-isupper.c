#include "main.h"
/**
 * _isupper - check if character is on uppercase
 * Return: return 1 if true in other case return 0
 */
int _isupper(int c)
{
	if (isupper(c))
	{
		return (1);
	}
	return (0);
}
